type Message {
  id: ID!
  text: String!
  user: String!
}

type Query {
  messages: [Message!]!
}

type Mutation {
  postMessage(user: String!, text: String!): Message!
}

type Subscription {
  messageAdded: Message!
}
